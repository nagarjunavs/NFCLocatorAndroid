package com.tapsense.app.data.settings

/** Persisted appearance preference - the real, user-controllable counterpart to `isSystemInDarkTheme()`. */
enum class AppearanceMode {
    SYSTEM,
    LIGHT,
    DARK,
}
