*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.hprof
.cxx/
.kotlin/
.navigation/
/*/release/

# Signing / publishing secrets - never commit these.
*.jks
*.keystore
key.properties
keystore.properties
signing.properties
secrets.properties
gradle.properties.local
*.asc
*.gpg
*.pem
*.p12

# Local Gradle build tuning some IDEs write per-machine.
.gradle/config.properties

# Personal publish-automation tooling, not library source - see RELEASING.md for the actual
# documented process this wraps. Contains no secrets itself (reads them from the environment),
# but a maintainer's local shortcut doesn't belong in a public library's history either way.
/scripts/publish-to-maven-central.sh

.github/
